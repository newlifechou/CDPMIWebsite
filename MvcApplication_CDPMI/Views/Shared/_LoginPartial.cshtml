@if (Request.IsAuthenticated)
{
    <text>
        你好，管理员，@Html.ActionLink(User.Identity.Name, "Manage", "Account", routeValues: null, htmlAttributes: new { @class = "username", title = "管理" })!
        @Html.ActionLink("进入管理界面", "Index", "Admin")
        @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
        {
            @Html.AntiForgeryToken()
            <a href="javascript:document.getElementById('logoutForm').submit()">注销</a>
        }
    </text>
}
else
{
    @*<ul>
        <li>@Html.ActionLink("登录", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>*@
    @Html.ActionLink("登录", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })
}
<a href="http://www.pioneer-materials.com" target="_blank" style="background-color:white;color:blue">English</a>
@*<a href="http://exmail.qq.com/login" target="_blank" style="background-color:white;color:blue">邮箱</a>*@