@model IEnumerable<MvcApplication_CDPMI.Models.basicSetting>
@{
    ViewBag.Title = "GetContactsView";
}

@foreach (var item in Model)
{
    <p>
        @Html.DisplayFor(o => item.title) :  @Html.DisplayFor(o => item.contentText)
    </p>
}
