@model MvcApplication_CDPMI.Models.friendLink

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>friendLink</legend>

        @Html.HiddenFor(model => model.id)

        <div class="editor-label">
            @Html.LabelFor(model => model.friendLinkTitle)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.friendLinkTitle)
            @Html.ValidationMessageFor(model => model.friendLinkTitle)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.friendLink1)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.friendLink1)
            @Html.ValidationMessageFor(model => model.friendLink1)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
