@model MvcApplication_CDPMI.Models.service

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>service</legend>

        @Html.HiddenFor(model => model.id)

        <div class="editor-label">
            @Html.LabelFor(model => model.serviceName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.serviceName)
            @Html.ValidationMessageFor(model => model.serviceName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.serviceContent)
        </div>
        <div class="editor-field">
            <script>
                var ue = UE.getEditor('serviceContent');
            </script>
            @Html.TextAreaFor(model => model.serviceContent)
            @Html.ValidationMessageFor(model => model.serviceContent)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.servicePicture)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.servicePicture)
            @Html.ValidationMessageFor(model => model.servicePicture)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
